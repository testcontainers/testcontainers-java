version: "2.1"
services:
    redis:
        image: redis
    mysql:
        image: mysql
    custom:
        build:
            context: compose-dockerfile
            dockerfile: Dockerfile
networks:
    custom_network: {}
