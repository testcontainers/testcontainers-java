
# This file is generated by .github/workflows/scripts/generate_github_ci.sh
# DO NOT HAND EDIT
name: CI

on:
  pull_request: {}
  push: { branches: [ master ] }

jobs:
    compile:
        runs-on: ubuntu-18.04
        needs: []
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan build -x test
    core:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan testcontainers:check
    selenium_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan selenium:check
    neo4j_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan neo4j:check
    clickhouse_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan clickhouse:check
    postgresql_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan postgresql:check
    dynalite_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan dynalite:check
    presto_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan presto:check
    jdbc-test_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan jdbc-test:check
    mariadb_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan mariadb:check
    mockserver_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan mockserver:check
    oracle-xe_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan oracle-xe:check
    rabbitmq_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan rabbitmq:check
    jdbc_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan jdbc:check
    influxdb_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan influxdb:check
    nginx_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan nginx:check
    kafka_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan kafka:check
    cassandra_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan cassandra:check
    mysql_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan mysql:check
    junit-jupiter_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan junit-jupiter:check
    mssqlserver_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan mssqlserver:check
    spock_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan spock:check
    db2_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan db2:check
    localstack_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan localstack:check
    cockroachdb_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan cockroachdb:check
    pulsar_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan pulsar:check
    database-commons_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan database-commons:check
    toxiproxy_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan toxiproxy:check
    elasticsearch_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan elasticsearch:check
    orientdb_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan orientdb:check
    vault_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan vault:check
    couchbase_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan couchbase:check
