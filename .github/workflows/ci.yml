
# This file is generated by .github/workflows/scripts/generate_github_ci.sh
# DO NOT HAND EDIT
name: CI

on:
  pull_request: {}
  push: { branches: [ master ] }

jobs:
    compile:
        runs-on: ubuntu-18.04
        needs: []
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info build -x test
    core:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info testcontainers:check
    selenium_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info selenium:check
    neo4j_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info neo4j:check
    clickhouse_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info clickhouse:check
    postgresql_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info postgresql:check
    dynalite_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info dynalite:check
    presto_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info presto:check
    jdbc-test_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info jdbc-test:check
    mariadb_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info mariadb:check
    mockserver_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info mockserver:check
    oracle-xe_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info oracle-xe:check
    rabbitmq_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info rabbitmq:check
    jdbc_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info jdbc:check
    influxdb_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info influxdb:check
    nginx_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info nginx:check
    kafka_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info kafka:check
    cassandra_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info cassandra:check
    mysql_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info mysql:check
    junit-jupiter_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info junit-jupiter:check
    mssqlserver_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info mssqlserver:check
    spock_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info spock:check
    db2_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info db2:check
    localstack_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info localstack:check
    cockroachdb_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info cockroachdb:check
    pulsar_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info pulsar:check
    database-commons_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info database-commons:check
    toxiproxy_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info toxiproxy:check
    elasticsearch_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info elasticsearch:check
    orientdb_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info orientdb:check
    vault_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info vault:check
    couchbase_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info couchbase:check
