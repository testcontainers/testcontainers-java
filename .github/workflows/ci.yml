
# This file is generated by .github/workflows/scripts/generate_github_ci.sh
# DO NOT HAND EDIT
name: CI

on:
  pull_request: {}
  push: { branches: [ master ] }

jobs:
    compile:
        runs-on: ubuntu-18.04
        needs: []
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel build -x test
    core:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel testcontainers:check
    selenium_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel selenium:check
    neo4j_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel neo4j:check
    clickhouse_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel clickhouse:check
    postgresql_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel postgresql:check
    dynalite_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel dynalite:check
    presto_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel presto:check
    jdbc-test_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel jdbc-test:check
    mariadb_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel mariadb:check
    mockserver_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel mockserver:check
    oracle-xe_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel oracle-xe:check
    rabbitmq_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel rabbitmq:check
    jdbc_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel jdbc:check
    influxdb_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel influxdb:check
    nginx_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel nginx:check
    kafka_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel kafka:check
    cassandra_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel cassandra:check
    mysql_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel mysql:check
    junit-jupiter_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel junit-jupiter:check
    mssqlserver_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel mssqlserver:check
    spock_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel spock:check
    db2_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel db2:check
    localstack_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel localstack:check
    cockroachdb_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel cockroachdb:check
    pulsar_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel pulsar:check
    database-commons_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel database-commons:check
    toxiproxy_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel toxiproxy:check
    elasticsearch_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel elasticsearch:check
    orientdb_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel orientdb:check
    vault_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel vault:check
    couchbase_module_test:
        runs-on: ubuntu-18.04
        needs: compile
        steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-java@v1
            with:
              java-version: '1.8'
          - name: Cache Gradle Home files
            uses: actions/cache@v1
            with:
              path: ~/.gradle
              key: ${{ runner.os }}-gradle-home-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-home
          - name: Cache Gradle Local files
            uses: actions/cache@v1
            with:
              path: .gradle
              key: ${{ runner.os }}-gradle-local-${{ hashFiles('**/*.gradle') }}
              restore-keys: ${{ runner.os }}-gradle-local
          - name: Build with Gradle
            run: |
                ./gradlew --no-daemon --continue --scan --info --parallel couchbase:check
