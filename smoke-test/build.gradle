// empty build.gradle for dependabot
plugins {
    id 'testcontainers.java-conventions'
}

apply from: "$rootDir/../gradle/ci-support.gradle"

subprojects {
    apply plugin: "java"
    apply plugin: 'testcontainers.java-conventions'

    repositories {
        mavenCentral()
    }
}
