rootProject.name = 'testcontainers-examples'

includeBuild '..'

include(
    'disque-job-queue',
    'linked-container',
    'redis-backed-cache',
    'redis-backed-cache-testng',
    'selenium-container',
    'spring-boot'
)

