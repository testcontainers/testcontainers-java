description = "TestContainers :: elasticsearch"

dependencies {
    compile project(':testcontainers')
    testCompile "org.elasticsearch.client:elasticsearch-rest-client:${elasticsearchVersion}"
}


processResources {
    expand(
        elasticsearchVersion: elasticsearchVersion
    )
}

processTestResources {
    expand(
        elasticsearchVersion: elasticsearchVersion,
        elasticsearchOldVersion: elasticsearchOldVersion
    )
}
